# bookbot

bookBot is my first [Boot.dev](https://www.boot.dev) project. When running main.py from the command line, it takes a text file (intentionally a book) as an argument, iterates over every character in it, and returns a count of each one.
